"""MiniDB - a tiny disk-backed relational database, built for learning.

Built one stage at a time. Stage 1 is the pager (minidb/pager.py): the only
component that reads or writes the database file.
"""
